{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci"
  },
  "deploy": {
    "startCommand": "npx tsx scripts/collect.ts --execute",
    "restartPolicyType": "NEVER"
  }
}
